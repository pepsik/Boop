<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:security="http://www.springframework.org/schema/security"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/security
       http://www.springframework.org/schema/security/spring-security.xsd">

    <import resource="classpath:spring/datasource-config.xml"/>

    <security:jdbc-user-service id="jdbcUserDetailsManager" data-source-ref="dataSource"
                                users-by-username-query="SELECT U.USERNAME,P.PASSWORD,TRUE FROM users U
                                                          JOIN passwords P ON  U.user_id = P.user_id_fk
                                                          WHERE U.username = ?"
                                authorities-by-username-query="SELECT  U.USERNAME,  R.ROLE FROM users U
                                                              INNER JOIN users_authority SA ON U.user_id = SA.user_id_fk
                                                              INNER JOIN ROLES R ON SA.ROLE_ID_FK = R.ROLE_ID
                                                              WHERE U.USERNAME = ? "/>

    <!--<security:cache-control/>-->
</beans>